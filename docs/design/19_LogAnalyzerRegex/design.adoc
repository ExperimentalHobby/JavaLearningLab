= 19_LogAnalyzerRegex 設計書
:doctype: article
:title-page:
:revdate: 2026-08-15
:toc: left
:toclevels: 2
:sectnums:
:pagenums:
:source-highlighter: rouge
:icons: font
:imagesdir: diagrams
:pdf-fontsdir: ../fonts
:pdf-themesdir: ../themes
:pdf-theme: ja

NOTE: UML図は `diagrams/` 配下の `.puml` ファイルを https://asciidoctor.org/docs/asciidoctor-diagram/[asciidoctor-diagram] 経由でPlantUMLとして埋め込んでいる。拡張未導入の環境では、各 `.puml` を直接 https://www.plantuml.com/plantuml[PlantUMLオンラインサーバー] 等に読み込ませても図を確認できる。PDF化する場合は同フォルダの `generate-pdf.ps1` を実行する(`docs/design/fonts/`・`docs/design/themes/` の日本語フォント・テーマを用いて文字化けを回避している。詳細は `docs/design/README.md` を参照)。

<<<

== 概要

=== 目的
`2026-08-13 10:15:30 [ERROR] メッセージ` 形式のログ行を正規表現で解析し、タイムスタンプ・ログレベル・メッセージを抽出する対話式CLIツールを提供する。`Pattern`/`Matcher` によるログ抽出の実践を目的とする。

=== 主な機能
* `load <ファイルパス>` でログファイルを読み込み、解析成功件数・スキップ件数・レベル別集計を表示する
* `exit` によるアプリ終了

=== 開発環境
[cols="1,3", options="header"]
|===
| 項目 | 内容

| 言語
| Java 25

| ビルドツール
| Maven(フォルダ単位で独立した `pom.xml`)

| テスト
| JUnit 5(`junit-jupiter`。`@TempDir` を用いた実ファイルでの結合テスト)

| 正規表現
| `java.util.regex.Pattern`/`Matcher`

| 集計
| `Stream`(`Collectors.groupingBy` + `Collectors.counting()`)
|===

=== 実行方法
[source,powershell]
----
cd 19_LogAnalyzerRegex
.\mvnw.cmd compile
.\mvnw.cmd test
.\mvnw.cmd exec:java "-Dexec.mainClass=com.javalab.loganalyzer.Main"
----

=== 関連資料
https://github.com/ExperimentalHobby/JavaLearningLab/issues/19[Issue #19] / https://github.com/ExperimentalHobby/JavaLearningLab/pull/49[PR #49]

<<<

== ユースケース

[cols="1,3,4", options="header"]
|===
| ID | ユースケース | 概要

| UC-1
| ログファイルを読み込み解析する
| `load <ファイルパス>` を入力し、ログ行をタイムスタンプ・レベル・メッセージへ解析する

| UC-2
| 解析結果を確認する
| 解析成功件数・スキップ件数を確認する

| UC-3
| レベル別集計を確認する
| ログレベルごとの件数集計を確認する

| UC-4
| 不正な入力時にエラーを表示する
| ファイル読み込み失敗・不明なコマンド時にエラーメッセージを表示しアプリを継続する

| UC-5
| アプリを終了する
| `exit` を入力し、対話ループを終了する
|===

<<<

== シーケンス図

=== ログファイルの読み込みと解析(正常系)
[plantuml, sequence-normal, svg]
----
include::diagrams/sequence-normal.puml[]
----

=== エラー処理
[plantuml, sequence-error, svg]
----
include::diagrams/sequence-error.puml[]
----

<<<

== クラス図
[plantuml, class-diagram, svg]
----
include::diagrams/class-diagram.puml[]
----

`LogParser` は正規表現で1行を解析して `LogEntry` を生成する。`parseAll` は1行ごとの解析失敗を内部で捕捉してスキップし、呼び出し元に例外を伝播させない。`countByLevel` はStream APIでレベルごとの件数を集計する。

<<<

== アクティビティ図
[plantuml, activity-diagram, svg]
----
include::diagrams/activity-diagram.puml[]
----

<<<

== 補足事項

=== 正規表現によるログ抽出
`LogParser` は `Pattern.compile("^(\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}) \[(\w+)\] (.+)$")` でコンパイルした正規表現を `Matcher` でマッチさせ、`group(1)`〜`group(3)` からタイムスタンプ・レベル・メッセージを抽出する。

=== 1行単位のスキップによる耐障害性
実際のログファイルにはスタックトレースの継続行など形式に合わない行が混ざりうるため、`parseAll` は1行の解析失敗(`LogParseException`)で全体を止めず、その行だけスキップして処理を継続する設計にしている。単発の `parse` は形式不正を例外として明確に返す。

=== Stream APIによる集計
`countByLevel` は `Stream` の `Collectors.groupingBy` + `Collectors.counting()` でレベルごとの件数を集計する(Issue #12で学んだStream APIの実践的な再利用)。

=== テスト方針(TDD)
Red → Green → Refactor サイクルで実装した。テストは実際のファイル(`@TempDir`)を使った結合テストとした(既存Issue #13/#18と同じ方針、モックなし)。

* *単体・結合テスト*(`LogParserTest`): 正常な行の解析、形式不正行のスキップ、レベル別集計を検証
* *結合テスト*(`MainTest`): `load`コマンドの結果表示、および存在しないファイル・不正なコマンド入力時にエラー表示してループを継続する動作を検証

=== 設計上の工夫
* `Main` は01〜18と同じ設計パターンで、`Scanner`/`PrintStream` を引数に取る `run` 静的メソッドに分離している。
