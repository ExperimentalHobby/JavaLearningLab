= 27_JpaHibernateApp 設計書
:doctype: article
:title-page:
:revdate: 2026-08-15
:toc: left
:toclevels: 2
:sectnums:
:pagenums:
:source-highlighter: rouge
:icons: font
:imagesdir: diagrams
:pdf-fontsdir: ../fonts
:pdf-themesdir: ../themes
:pdf-theme: ja

NOTE: UML図は `diagrams/` 配下の `.puml` ファイルを https://asciidoctor.org/docs/asciidoctor-diagram/[asciidoctor-diagram] 経由でPlantUMLとして埋め込んでいる。拡張未導入の環境では、各 `.puml` を直接 https://www.plantuml.com/plantuml[PlantUMLオンラインサーバー] 等に読み込ませても図を確認できる。PDF化する場合は同フォルダの `generate-pdf.ps1` を実行する(`docs/design/fonts/`・`docs/design/themes/` の日本語フォント・テーマを用いて文字化けを回避している。詳細は `docs/design/README.md` を参照)。

<<<

== 概要

=== 目的
JPA(Jakarta Persistence)アノテーションを付与した`Product`エンティティを、Hibernateを介してH2組み込みインメモリDBに永続化する在庫管理CRUDコンソールアプリを提供する。生SQLを書かず、アノテーションとEntityManagerのAPIだけでCRUD操作を組み立てる体験(アノテーションベースのDB連携)に主眼を置く。

=== 主な機能
* `add <商品名> <価格> <在庫数>` による商品の新規登録(IDは自動採番)
* `list` による全商品の一覧表示
* `find <id>` によるID指定での1件取得
* `update <id> <価格> <在庫数>` による既存商品の価格・在庫数の更新
* `delete <id>` による指定IDの商品削除
* `exit` によるアプリ終了

=== 開発環境
[cols="1,3", options="header"]
|===
| 項目 | 内容

| 言語
| Java 25

| ビルドツール
| Maven(フォルダ単位で独立した `pom.xml`)

| テスト
| JUnit 5(`junit-jupiter` 5.11.3)

| ORM
| Hibernate(`hibernate-core` 7.4.5.Final、JPA/Jakarta Persistenceアノテーション)

| DB
| H2組み込みインメモリDB(`h2` 2.4.240、`jdbc:h2:mem:productdb`)
|===

=== 実行方法
[source,powershell]
----
cd 27_JpaHibernateApp
.\mvnw.cmd compile
.\mvnw.cmd test
.\mvnw.cmd exec:java "-Dexec.mainClass=com.javalab.jpahibernate.Main"
----

=== 関連資料
https://github.com/ExperimentalHobby/JavaLearningLab/issues/27[Issue #27] / https://github.com/ExperimentalHobby/JavaLearningLab/pull/62[PR #62]

<<<

== ユースケース

[cols="1,3,4", options="header"]
|===
| ID | ユースケース | 概要

| UC-1
| 商品を新規登録する
| `add <商品名> <価格> <在庫数>` を入力し、IDが自動採番された商品を登録する

| UC-2
| 商品一覧を表示する
| `list` を入力し、登録済み全商品を一覧表示する

| UC-3
| 商品をID指定で取得する
| `find <id>` を入力し、該当商品の内容を表示する

| UC-4
| 商品を更新する
| `update <id> <価格> <在庫数>` を入力し、既存商品の価格・在庫数を更新する

| UC-5
| 商品を削除する
| `delete <id>` を入力し、指定IDの商品を削除する

| UC-6
| 該当商品が見つからない場合にエラーを表示する
| `find`/`update`/`delete`で存在しないIDを指定した場合、エラーメッセージを表示する

| UC-7
| 不明なコマンド時にエラーを表示する
| `add`/`list`/`find`/`update`/`delete`/`exit`以外のコマンドを入力した場合、エラーメッセージを表示する

| UC-8
| アプリを終了する
| `exit` を入力し、対話ループを終了する
|===

<<<

== シーケンス図

=== 商品の登録・一覧・取得(正常系)
[plantuml, sequence-normal, svg]
----
include::diagrams/sequence-normal.puml[]
----

=== 更新・削除処理と該当商品なしエラー
[plantuml, sequence-error, svg]
----
include::diagrams/sequence-error.puml[]
----

<<<

== クラス図
[plantuml, class-diagram, svg]
----
include::diagrams/class-diagram.puml[]
----

`Main` は `ProductRepository` にCRUD操作を委譲する。`ProductRepository` はコンストラクタで受け取った `EntityManager` をそのまま使い回し、更新系操作(`save`/`update`/`deleteById`)では明示的にトランザクションを開始・コミットする。`Product` は `@Entity`/`@Id`/`@GeneratedValue`/`@Column` を付与したJPAエンティティであり、生成はコンストラクタ経由に限定している。

<<<

== アクティビティ図
[plantuml, activity-diagram, svg]
----
include::diagrams/activity-diagram.puml[]
----

<<<

== 補足事項

=== アノテーションベースのマッピング
`Product`は`@Entity`/`@Table`/`@Id`/`@GeneratedValue`/`@Column`をフィールドに直接付与するアノテーションベースのマッピングとした。JPA仕様上、エンティティにはpublic/protectedの引数無しコンストラクタが必須なため、`protected Product()`を用意しつつ、通常利用は3引数コンストラクタに限定した。

=== 16_JdbcCrudAppとの対比
永続化ユニット(`persistence.xml`)はH2インメモリDB(`jdbc:h2:mem:productdb`)を指し、`hibernate.hbm2ddl.auto=create-drop`によりテスト・実行のたびにスキーマが作り直されるようにした。16_JdbcCrudAppの生SQLベースの実装と対比できるよう、あえて別のDB(SQLite→H2)を採用した。

=== 手動トランザクション管理
`ProductRepository`はコンストラクタで受け取った`EntityManager`をそのまま使い回し、`save`/`update`/`deleteById`では明示的にトランザクションを開始・コミットしている(`RESOURCE_LOCAL`のためコンテナ管理トランザクションではなく手動管理が必要)。

=== テスト方針(TDD)
Red → Green → Refactor サイクルで実装した。テストは全て実際のH2インメモリDBに対して行い、モックは使用していない(他の学習フォルダと同じ「実リソースで確認する」方針を踏襲)。`@BeforeEach`でテストごとに新しい`EntityManagerFactory`を生成し、`@AfterEach`でクローズすることでテスト間のデータ汚染を防いでいる。

* *単体テスト*(`ProductRepositoryTest`): save/findById/findAll/update/deleteByIdの正常系、存在しないIDでのfind/update/deleteの挙動を検証
* *結合テスト*(`MainTest`): `Scanner`/`PrintStream`/`ProductRepository`を注入した対話ループに対して、add→list→find→update→deleteの一連の流れ、存在しないIDへの操作時のエラー表示を検証
