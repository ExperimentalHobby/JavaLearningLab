= 16_JdbcCrudApp 設計書
:doctype: article
:title-page:
:revdate: 2026-08-15
:toc: left
:toclevels: 2
:sectnums:
:pagenums:
:source-highlighter: rouge
:icons: font
:imagesdir: diagrams
:pdf-fontsdir: ../fonts
:pdf-themesdir: ../themes
:pdf-theme: ja

NOTE: UML図は `diagrams/` 配下の `.puml` ファイルを https://asciidoctor.org/docs/asciidoctor-diagram/[asciidoctor-diagram] 経由でPlantUMLとして埋め込んでいる。拡張未導入の環境では、各 `.puml` を直接 https://www.plantuml.com/plantuml[PlantUMLオンラインサーバー] 等に読み込ませても図を確認できる。PDF化する場合は同フォルダの `generate-pdf.ps1` を実行する(`docs/design/fonts/`・`docs/design/themes/` の日本語フォント・テーマを用いて文字化けを回避している。詳細は `docs/design/README.md` を参照)。

<<<

== 概要

=== 目的
タスク管理(id・タイトル・完了状態)を題材にしたCRUD操作の対話式CLIツールを提供する。SQLite連携とJDBCによるCRUD操作の実践を目的とする。

=== 主な機能
* `add <タイトル>` でタスクを登録する
* `list` でタスク一覧を表示する
* `done <id>` でタスクを完了にする
* `delete <id>` でタスクを削除する
* `exit` によるアプリ終了

=== 開発環境
[cols="1,3", options="header"]
|===
| 項目 | 内容

| 言語
| Java 25

| ビルドツール
| Maven(フォルダ単位で独立した `pom.xml`)

| テスト
| JUnit 5(`junit-jupiter`。`jdbc:sqlite::memory:` の実DBに対する結合テスト)

| DB
| SQLite(`org.xerial:sqlite-jdbc`)、本番は `jdbc:sqlite:tasks.db`
|===

=== 実行方法
[source,powershell]
----
cd 16_JdbcCrudApp
.\mvnw.cmd compile
.\mvnw.cmd test
.\mvnw.cmd exec:java "-Dexec.mainClass=com.javalab.jdbccrud.Main"
----

=== 関連資料
https://github.com/ExperimentalHobby/JavaLearningLab/issues/16[Issue #16] / https://github.com/ExperimentalHobby/JavaLearningLab/pull/46[PR #46]

<<<

== ユースケース

[cols="1,3,4", options="header"]
|===
| ID | ユースケース | 概要

| UC-1
| タスクを登録する
| `add <タイトル>` を入力し、未完了タスクとしてDBに登録する

| UC-2
| タスク一覧を表示する
| `list` を入力し、登録済みタスクをID昇順で表示する

| UC-3
| タスクを完了にする
| `done <id>` を入力し、対象タスクの完了状態を更新する

| UC-4
| タスクを削除する
| `delete <id>` を入力し、対象タスクをDBから削除する

| UC-5
| 不正な入力・DB操作失敗時にエラーを表示する
| DB操作の失敗・不正なID・該当タスクなし・不明なコマンド時にエラーメッセージを表示しアプリを継続する

| UC-6
| アプリを終了する
| `exit` を入力し、対話ループを終了する
|===

<<<

== シーケンス図

=== タスクのCRUD操作
[plantuml, sequence-normal, svg]
----
include::diagrams/sequence-normal.puml[]
----

=== エラー処理
[plantuml, sequence-error, svg]
----
include::diagrams/sequence-error.puml[]
----

<<<

== クラス図
[plantuml, class-diagram, svg]
----
include::diagrams/class-diagram.puml[]
----

`TaskRepository` は `tasks` テーブルに対するCRUD操作をJDBCで実行し、`SQLException` は `TaskRepositoryException` に変換して呼び出し元へ伝える。`Connection` はコンストラクタで注入され、テストと本番で異なる接続先を差し替えられる。

<<<

== アクティビティ図
[plantuml, activity-diagram, svg]
----
include::diagrams/activity-diagram.puml[]
----

<<<

== 補足事項

=== SQLite採用理由
サーバー不要でファイル/インメモリで完結するSQLiteを採用している。MySQLと異なり起動中のDBサーバーが不要で、既存exercise(#13〜#15)と同じ「モックなし・実リソースでの結合テスト」方針に自然に合うため。

=== DI-for-testabilityパターン
`TaskRepository` はコンストラクタで `Connection` を注入するDI-for-testabilityパターンを踏襲している。テストは `jdbc:sqlite::memory:` の実DBに対して行い、本番は `jdbc:sqlite:tasks.db` のファイルDBを使う。

=== 例外の一本化と更新行数による存在判定
個々のCRUDメソッドの失敗は `SQLException` を `TaskRepositoryException`(非チェック例外)に変換して統一し、呼び出し側のcatch節を1つに集約している。`updateDone`/`delete` は `PreparedStatement#executeUpdate` の戻り値(更新/削除された行数)が0かどうかで、対象IDが存在しなかった場合を判別している。

=== sqlite-jdbcのJNI警告について
`sqlite-jdbc` はJNIでネイティブライブラリを読み込むため、テスト実行時にJDK 25の「制限付きメソッド」に関する警告(`WARNING: A restricted method in java.lang.System has been called`)が出力される。これはアプリケーションコード側で制御できないライブラリ内部の `System.load` 呼び出しに起因するものであり、テスト結果やビルドには影響しない。

=== テスト方針(TDD)
Red → Green → Refactor サイクルで実装した。

* *単体・結合テスト*(`TaskRepositoryTest`): インメモリSQLiteに対する登録・一覧取得・完了更新・削除、該当なしの判定を検証
* *結合テスト*(`MainTest`): `add`/`list`/`done`/`delete`コマンドの結果表示、および不正なコマンド入力時にエラー表示してループを継続する動作を検証

=== 設計上の工夫
* `Main` は01〜15と同じ設計パターンで、`Scanner`/`PrintStream`/`TaskRepository` を引数に取る `run` 静的メソッドに分離している。
